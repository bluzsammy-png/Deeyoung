// part0.js — Executive Summary & Audit Method
module.exports = function build(H) {
  const { h1, h2, p, bullets, note } = H;
  const out = [];

  out.push(h1("Executive Summary"));
  out.push(p("QuantEdge Pro, as deployed at the audited URL, is a polished and unusually honest single-user day-trading paper terminal. It already delivers most of the surface experience the Master Upgrade Prompt asks for: a five-screen unified product (Dashboard, Charts & Signals, Universe, AI Bot, Manual), live United States equity data proxied server-side through Yahoo Finance, an AI desk briefing and per-symbol analysis generated by a real server-side language model, a seven-factor signal engine with visible factor contributions and ATR-based trade plans, and a permission-gated SENTINEL bot that ships switched off in SIGNAL_ONLY mode behind a deterministic risk governor (1% risk per trade, five concurrent positions, a 3% daily-loss circuit breaker, a 0.60 minimum confidence, and 600-second approval expiry - all verified directly from the bot configuration API)."));
  out.push(p("The verdict of this audit is nonetheless clear: **the product is not production-ready as a multi-user service, and it is not safe to charge money for in its current state**. Three classes of blockers were verified during the audit. First, security: the application has no authentication whatsoever, and mutation endpoints respond to unauthenticated requests - during the audit, a forged POST to the bot tick endpoint successfully ran the trading engine, and a forged POST to the trade action endpoint was processed by business logic rather than rejected. Second, state model: the paper account, bot configuration, trades, and signals live in a single global record shared by every visitor, so any two people using the site at the same time are trading the same book. Third, missing subsystems: there is no news or catalyst engine, no notifications, no backtesting, no strategy lab, no billing, no admin console, and no per-user risk profiles."));
  out.push(p("At the same time, the audit strongly confirms the instruction to refactor rather than rebuild (Section 61). The two-brain philosophy, the permission-gated bot, the observe-by-default stance, the factor-transparency of the signal engine, the session playbook, the Reality Check marketing section, and the restrained dark terminal design language are all precisely the identity the upgrade prompt demands, and approximately two thirds of the existing implementation survives into the target architecture through wrapping, hardening, and extraction rather than replacement. The recommended path is an eleven-phase program (Phase 0 through Phase 10) on a zero-cost-first stack: managed PostgreSQL on a free tier, a free-tier news provider integrated behind a cost gate, free web push and email infrastructure, and AI usage metering with a bring-your-own-key option. Eight material decisions - including multi-user timing, news provider budget, and subscription model - are isolated in Part IV and require your confirmation before Phase 1 begins."));

  out.push(h1("Audit Scope, Method, and Evidence"));
  out.push(p("This audit was performed as a black-box engagement against the live deployment, because the current sandbox does not contain the application source code. Four evidence streams were collected on September 2, 2026, between 15:41 and 16:00 UTC: (1) direct probing of ten backend API endpoints with curated requests, including two deliberate unauthenticated mutation attempts; (2) static analysis of the seven largest served JavaScript bundles (approximately 1.17 MB combined) to enumerate API surface, storage usage, and external dependencies; (3) a full walkthrough of the five product screens captured as reference screenshots; and (4) a complete extraction and review of the marketing site copy."));
  out.push(p("Throughout this report, statements are tagged by confidence. **Verified** means reproduced first-hand during the audit (for example, an API response or a server behavior). **Per UI** means observed in screenshots or on-page copy. **Inferred** means an architectural judgment from observed behavior that source inspection should later confirm. This convention matters because Section 61 of the mandate requires the audit to distinguish what already works from what is assumed, and because the Phase 1 team should re-verify every inferred item against the source before touching it."));
  out.push(note("Scope boundary: performance profiling under load, dependency vulnerability scanning, and a line-by-line source review were out of reach for a black-box pass and are scheduled as Phase 1 entry tasks. Nothing in this report should be read as certifying code quality beyond observed behavior."));
  return out;
};
